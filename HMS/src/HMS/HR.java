package HMS;

public class HR extends Employee {
    public HR(){
        super();
    }
    public HR(int employeeId, String loginId,String password,String fullName, String role){
        super(employeeId, loginId, password, fullName, role);
    }
}
